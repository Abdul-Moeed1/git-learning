name : Hello Workflow

on:
 push:
  branches: [ main ]

jobs:
 demo:
  runs-on: ubuntu-latest

  concurrency:
    group: newgroup
    cancel-in-progress: true

  steps:
    - name: checkout
      uses: actions/checkout@v6.1.0
    - name: greeting
      run: echo "Hello Pakistan"
